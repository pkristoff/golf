<%= render 'share/error_messages', formats: [:html], resource: @round %>

<%= render "rounds/editable_fields", course: @course, tee: @tee, round: @round, form_disabled: form_disabled %>

<%= render "rounds/scores", round: @round, tee: tee, score_holes: @round.score_holes, form_disabled: form_disabled %>

