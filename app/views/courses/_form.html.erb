<%= render 'share/error_messages.html.erb', resource: course %>

<%= render 'courses/editable_fields.html.erb', form_disabled: form_disabled, course: course %>

<%= render "tees/tees", tees: course.sorted_tees %>