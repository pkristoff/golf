<%= render 'share/error_messages', formats: [:html], resource: @tee %>

<%= render "holes/editable_fields", course: @course, tee: @tee, hole: @hole %>

<%= render "holes/holes", tee: @tee, holes: holes %>

